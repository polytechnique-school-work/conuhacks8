#!/usr/bin/env bash
set -e
if [[ ! -d "/home/marcantoinem/Projects/rust/conuhacks8/frontend" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/marcantoinem/Projects/rust/conuhacks8/frontend")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/marcantoinem/Projects/rust/conuhacks8/frontend" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/marcantoinem/Projects/rust/conuhacks8/frontend/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/marcantoinem/Projects/rust/conuhacks8/frontend/.envrc" "/home/marcantoinem/Projects/rust/conuhacks8/frontend/.direnv"/*.rc
